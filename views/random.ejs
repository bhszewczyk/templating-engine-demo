<%- include('partials/head') %>
	<body>
		<%- include('partials/nav') %>
		<h1>Your random number is: <%= rand %></h1>
		<% if(rand % 2 === 0) {%>
		<h2>Even number!</h2>
		<% }else {%>
		<h2>Odd number!</h2>
		<% } %>
	</body>
</html>
